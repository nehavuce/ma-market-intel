# Copy this to secrets.toml for local dev, or paste into
# Streamlit Cloud's Secrets UI at:
#   https://share.streamlit.io → Your app → Settings → Secrets

ANTHROPIC_API_KEY = "sk-ant-..."

# Optional: password-protect the app
# APP_PASSWORD = "your-team-password"
